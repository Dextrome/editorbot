# AI Audio Editor - Dependencies

# Core audio processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
scipy>=1.10.0
numpy>=1.24.0

# AI/ML
torch>=2.0.0
torchaudio>=2.0.0

# Music structure analysis
allin1>=1.0.0  # ML-based music structure analyzer (beat, segment, label detection)
demucs>=4.0.0  # Source separation (used by allin1)
imageio-ffmpeg>=0.4.8  # Provides a bundled ffmpeg binary when system ffmpeg isn't present
# natten - Required for GPU acceleration, must be built from source on Windows:
#   git clone https://github.com/SHI-Labs/NATTEN.git
#   set NATTEN_CUDA_ARCH=<your_arch>  (e.g., 8.9 for RTX 4070 Ti)
#   pip install . --no-build-isolation

# Development
pytest>=7.3.0
pytest-cov>=4.0.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0
flake8>=6.0.0

# Type stubs
types-setuptools
